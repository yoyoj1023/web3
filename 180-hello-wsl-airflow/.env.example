# 複製為 .env 後依你的 WSL2 使用者調整
# cp .env.example .env
# 或直接：echo "AIRFLOW_UID=$(id -u)" > .env

AIRFLOW_UID=1000

# 選用：自訂 Web UI 登入帳密（預設 airflow / airflow）
# _AIRFLOW_WWW_USER_USERNAME=airflow
# _AIRFLOW_WWW_USER_PASSWORD=airflow
